#include "holberton.h"

/**
 * _abs - Calculates the absolute value
 * @n: number to be calculated
 *
 * Return: Absolute value of n
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
